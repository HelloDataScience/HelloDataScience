# Jekyll 로컬 빌드 산출물
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/

# bundler
# /vendor/bundle/ 만 막으면 macOS가 만드는 중복 사본("vendor 2/")이 걸러지지 않아
# git status에 계속 뜬다. vendor 로 시작하는 루트 폴더를 통째로 무시한다.
/vendor/
/vendor */

.bundle/

# macOS
.DS_Store
